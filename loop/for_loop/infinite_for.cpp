/*
This program will run infinitely, because as in maths "where there is no value there's 1 (TRUE)."
So the for loop will always stay true, the program will run infinitely.
*/

#include <iostream>

int main() {
  
  for( ; ; ){
    
    std::cout << "infinite" << std::endl;
  }

}
